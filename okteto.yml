build:
 icec-crud:  # cambiar por su namespace de okteto
   image: okteto.dev/icec-crud:latest  # cambiar por su namespace de okteto

deploy:
 - okteto build # build the image with the latest code
 - kubectl apply -f manifests # create all the resources in kubernetes
dev:
 icec-crud:
   command: bash
   sync:
   - .:/api-crud-users   # cambiar por su proyecto